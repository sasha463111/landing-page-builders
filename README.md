# Landing Page Builders - React App

דף נחיתה מודרני ב-React עבור סשה דיבקה - איש אוטומציה ויזם.

## 🚀 התקנה והפעלה

### דרישות
- Node.js 16+ (מומלץ 18+)
- npm או yarn

### התקנת תלויות
```bash
npm install
```

### הפעלה מקומית (Development)
```bash
npm run dev
```
האפליקציה תהיה זמינה ב: `http://localhost:5173`

### Build ל-Production
```bash
npm run build
```
הקבצים יווצרו בתיקייה `dist/`

### Preview של ה-Build
```bash
npm run preview
```

## 📁 מבנה הפרויקט

```
landing-page-builders/
├── src/
│   ├── components/          # קומפוננטות React
│   │   ├── Header.jsx
│   │   ├── Hero.jsx
│   │   ├── Problem.jsx
│   │   ├── About.jsx
│   │   ├── Solution.jsx
│   │   ├── Process.jsx
│   │   ├── ContactForm.jsx
│   │   └── Footer.jsx
│   ├── styles/              # קבצי CSS
│   │   ├── index.css        # סגנונות כלליים
│   │   ├── Header.css
│   │   ├── Hero.css
│   │   └── ...
│   ├── App.jsx              # קומפוננטה ראשית
│   └── main.jsx             # נקודת כניסה
├── index.html               # HTML ראשי
├── package.json             # תלויות והגדרות
├── vite.config.js           # הגדרות Vite
└── vercel.json              # הגדרות Vercel
```

## 🎨 עיצוב

- **פונט**: Heebo (מ-Google Fonts)
- **צבעים**: 
  - ירוק ראשי: `#34D399`
  - רקע שחור: `#000000`
  - רקע כהה: `#111111`
- **רספונסיבי**: Mobile, Tablet, Desktop

## 📦 Deployment ב-Vercel

הפרויקט מוכן ל-deployment ב-Vercel:

1. חבר את ה-repository ל-Vercel
2. Vercel יזהה אוטומטית את זה כ-Vite React app
3. הוא יריץ `npm install` ו-`npm run build` אוטומטית
4. הדף יהיה זמין תוך כמה שניות!

## 🔧 טכנולוגיות

- **React 18** - ספריית UI
- **Vite** - Build tool מהיר
- **CSS Modules** - סטיילים מודולריים

## 📝 עדכונים

להעלאת עדכונים:
```bash
./deploy.sh "תיאור השינוי"
```

או ידנית:
```bash
git add .
git commit -m "תיאור השינוי"
git push
```

Vercel יעדכן אוטומטית!

## 🐛 פתרון בעיות

### שגיאת npm לא נמצא
התקן Node.js מ-[nodejs.org](https://nodejs.org)

### שגיאת Port בשימוש
Vite משתמש ב-port 5173 כברירת מחדל. אם הוא תפוס, Vite יבחר port אחר אוטומטית.

## 📄 רישיון

כל הזכויות שמורות © סשה דיבקה